#include <stdio.h>

/**
 *
 * main- Entry point, where the program begins
 *
 * Return: Always 0 (Success), where it ends
 *
 */

int main(void)
{
	int d = 0;
	
	while (d < 10);
	{
		printf("%i", d);
		d++;
	}
	putchar('\n');

		return (0);
}
