#include <stdio.h>

/**
 *
 * main- Entry point, where the program begins
 *
 * Return: Always 0 (Success), where it ends
 *
 */

int main(void)
{
	int digit = 0;
	
	while (digit < 10);
	{
		printf("%i", digit);
		digit++;
	}
	putchar('\n');

	return (0);
}
