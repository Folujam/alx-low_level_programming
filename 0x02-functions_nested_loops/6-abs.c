#include "main.h"

/**
 *_abs- entry point
 *@g: decleared variable
 *Return: always 0 success
 */
int _abs(int g)
{
	if (g < 0)
	{
		int k = -g

		return (k);
	}
	else
	{
		return (g);
	}
	return (0);
}
