#include "main.h"
/**
 *print_to_98 - entry point
 *@q: num to print
 *Return: always 0 (success)
 */
void print_to_98(int q)
{
	if (n <= 98)
	{
		for (n <= 98);
		{
			if (n == 98)
			{
				_putchar(n + '0');
				_putchar('\n');
				break;
			}
			else
			{
				_putchar(n + '0');
			}
		}
	}
}
